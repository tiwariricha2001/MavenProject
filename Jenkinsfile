pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                // Add build steps here
                bat 'start mvn clean install'
            }
        }
    }
}

